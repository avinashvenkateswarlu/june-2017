<?xml version="1.0" encoding="UTF-8"?>
<pattern-mapping>
    
    <pattern name="pattern_2" type="code"> <!--regexp -->
        <!-- If type is code -->
        <imports>
            import java.util.*;
        </imports>
        <code>
            if(input.equalsIgnoreCase("51632272"))
            {
                return true;
            }
        </code>
        <function>
            static String display(String a)
            {
                return a.toUpperCase();
            }
        </function>
    </pattern>
    
    <pattern name="employee_id" type="code">
        <file lang="java" path-type="class" class="com.hcl.ai_bot.customPattern.Phone"/>
    </pattern>
    
    <pattern name="employee_id51" type="regexp"> <!--regexp -->
        <!-- If regexp -->
        <exp>java regular expression</exp>
        <!-- End of regexp -->
    </pattern>
    <!--
        Planning to load class files in remote servers using url's custom class loader defineclass()
        Waiting for opinion on this.
        -->    
</pattern-mapping>
